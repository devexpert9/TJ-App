import { EventEmitter, ElementRef, Renderer2, ChangeDetectorRef } from '@angular/core';
export declare class NgxFlagPickerComponent {
    private renderer;
    private changeDetectorRef;
    private _isShowListCountryFlags;
    set isShowListCountryFlags(value: boolean);
    get isShowListCountryFlags(): boolean;
    selectedCountryCode: string;
    countryCodes: string[];
    customLabels: Record<string, string>;
    showFlags: boolean;
    showLabels: boolean;
    showArrow: boolean;
    changedCountryCode: EventEmitter<string>;
    selectFlagsElementRef: ElementRef;
    outsideClickSelectFlags: () => void;
    constructor(renderer: Renderer2, changeDetectorRef: ChangeDetectorRef);
    getCountryLabel(countryCode: string): string;
    toggleListCountryFlags(): void;
    private openListCountryFlags;
    private closeListCountryFlags;
    changeSelectedCountryCode(value: string): void;
    private subscribeOutsideClickSelectFlags;
    private unsubscribeOutsideClickSelectFlags;
}
